import "dotenv/config"; import { eq } from "drizzle-orm"; import { db, pool } from "./index"; import { movies } from "./schema"; import { seedMovies } from "./seed-data/movies"; async function main() { for (const m of seedMovies) { await db.update(movies).set({ releaseYear: m.releaseYear, durationMinutes: m.durationMinutes, director: m.director, writer: m.writer || null, cast: m.cast }).where(eq(movies.slug, m.slug)); } console.log("done"); await pool.end(); } main();
