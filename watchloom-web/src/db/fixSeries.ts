import "dotenv/config"; import { eq } from "drizzle-orm"; import { db, pool } from "./index"; import { series } from "./schema"; import { seedSeries } from "./seed-data/series"; async function main() { for (const s of seedSeries) { await db.update(series).set({ releaseYear: s.releaseYear, status: s.status, network: s.network, creator: s.creator, cast: s.cast }).where(eq(series.slug, s.slug)); } console.log("done"); await pool.end(); } main();
